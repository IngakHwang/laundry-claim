# 🤝 HANDOFF r11 — 세탁 상담 챗 (노트북 → 데스크탑): sources 수정 배포 + 사진 확인 완료

> 2026-08-11 오후, **노트북(laundry-stain) 세션이 작성.** r10 보고 2건을 처리했다 —
> 근거 칩은 고쳐서 배포했고, 사진 저장분은 요청 항목 전부 확인했다.

## 1. sources 빈 배열 — 회귀가 아니라 **원래 있던 구멍**이었다 (수정·배포 완료)

- 시점 반증: **2026-08-10 19:24 노트북 로그**(데스크탑 종단 시험 흔적)의 같은 질문
  "커피 얼룩은 어떻게 빼나요?"도 `cited: []` — 즉 얼룩 지식 인용은 **v2(얼룩 편입) 배포
  때부터 sources에 실린 적이 없다.** 오늘 이미지 배포와 무관하다.
- 오전에 "정상"으로 보인 사례는 후속 질문("그럼 찬물로도…")이 **조언11**을 인용한 경우 —
  조언 번호 인용만 sources에 실리는 구조였고, 유형이 달랐던 것.
- 수정: 서버 응답 조립에서 얼룩 인용(stain_cited)도 sources에 싣는다. **LLM 파일 무변경**
  (서버 계층) — 합격선 재시험 비대상. 배포·실서버 확인 완료:

```json
얼룩 인용:  [{"stain": "커피·차·음료", "title": "얼룩-커피·차·음료", "badge": true}]
조언 인용:  [{"n": 11, "title": "지금은 표백제를 늘려도 소용이 없습니다", "badge": true}]  ← 기존 그대로
```

- ⚠ 칩 렌더러 확인 1건: 얼룩 행에는 `n`이 **없다** (`stain` 필드가 대신 있음). 렌더러가
  `title`만 쓰면 그대로 뜰 것이고, `조언${n}` 식으로 조립한다면 얼룩 행 분기가 필요하다.
  얼룩 지식은 전부 조사 근거라 `badge`는 고정 true다.

## 2. 사진 저장분 확인 (r10 §1 요청) — 전부 정상

- 종단 시험 2장 모두: **1067×1600 세로** ✓ (Orientation=6이 픽셀로 반영됨 — exif_transpose 작동)
  · **EXIF 0건** ✓ · JPEG ✓ · 27,429바이트.
- `collected.jsonl`: 오늘 4행 (노트북 로컬 시험 2 + 종단 2). 시각·bytes·question 기록 정상,
  `turns`도 0/2로 이력 상황 반영됨.

## 3. 다음

- 데스크탑: 근거 칩 재확인(얼룩 행 렌더링 포함)만 하면 된다. 그것까지 통과하면 **이미지
  1단계 완료 선언에 동의** — 폰 실기기 촬영 확인은 인각님 몫으로 남긴다.
