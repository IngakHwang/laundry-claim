# 2026-08-10 · laundry-ticket

> 이 날의 작업 기록. **"왜"만 적는다** — "무엇이 바뀌었나"는 git 이력에 있다.
> 아직 안 끝난 것은 `..\BATON.md`에. **한번 적으면 안 고친다.**

## laundry-ticket

- 세션 시작, 오늘 할 일 정리 요청. BATON 기준 남은 일 셋(이력서 링크 반영·GitHub About 란·README 캡처) 확인. 추천 순서 = About 란 → 캡처, 이력서 반영은 라이넨스 지원 세션과 겹치므로 그쪽으로 미루는 쪽 제안 — 이유: About 란은 혼자 5분에 끝나고, 이력서는 프로젝트 하나가 아니라 이력서 전체를 만지는 일이라 이 세션 범위를 넘는다. 아직 인각님 선택 대기 중.
- 방향 전환: 남은 잔일 대신 **v2 착수** 결정(인각님). 기존 기능 목록을 코드에서 뽑아 정리하고, v2 후보 셋 중 **Postgres 이관 우선**을 제안 — 이유: 셋 중 이력서 경험 카드가 되는 건 이관뿐이고, 인증은 데이터가 영속해야 실익이 생기며, SMS는 유료·발신번호 등록이라는 외부 절차에 걸린다. 인각님 선택 대기.
- v2 방향 확정(인각님): **①UI/UX 점검·디자인 업그레이드 먼저 ②DB는 무료 Postgres(카드 등록 없는 서비스만 — 과금 원천 차단) ③알림은 SMS 대신 카톡, 무료 범위만**. UI/UX는 "만든 눈"이 못 보는 어색함이 걱정이라 검증 분리 요청 — 방식 = 폰 폭 스크린샷 + 수정 이력을 모르는 다중 리뷰어 병렬 점검, 겹치는 지적을 진짜 문제로 채택, 고친 뒤 새 눈으로 재검증.
- UI/UX 점검 완료(리뷰어 5 + 디자인 방향 1, 병렬·상호 비공개): 원지적 65건→병합 41건. 최다 겹침(4명) = 폰 상단 내비 잘림으로 「내 클레임」 발견 불가 — 폰 우선 원칙이 내비에서 무너져 있었다는 뜻. 디자인 진단 = "뼈대는 이미 플로우와 같음, 표면 셋(이모지·맑은고딕·초록 톤)이 인상을 깎음 → 구조 유지·재질 교체". 첫 라운드 12건 + 재질 교체 순서로 로드맵을 보고서(artifact)로 발행, 인각님 범위 선택 대기.
- 인각님 선택 = "기반+12건부터(단계마다 확인)". 반영 완료 — CSS 색 토큰화 + 12건. 결정 둘: ①확인 대기 색은 회색이 아니라 **주황** — "아직 아무도 안 본 것"이 눈에서 물러나면 안 되고, 빨강만 긴급에 반납하면 축 분리는 충분. ②상태명 「접수」→「확인 대기」는 화면 표시만 — DB 코드(new)와 CLAUDE.md의 상태 흐름 표기는 안 건드림(규칙 파일은 선설명·후승인 원칙). 1차 검증 = 로그인 리다이렉트 3역할 기능 테스트 + 동일 조건 스크린샷 14장 재촬영. 새 눈 재검증은 재질 교체 뒤 한 번에.
- 전후 비교는 두 버전 동시 기동(수정 전 = git 이력에서 워크트리로 꺼내 8766)으로 제공 — 캡처만으론 "직접 눌러보는" 확인이 안 되므로. 인각님 PC 확인 후 피드백 1건: **담당자별 클레임에 담당자마다 미완료 건수를 숫자로** → 즉시 반영(미완료 N건·긴급 N 배지 + 공장별 그룹핑, 점검 15번 지적의 그룹핑 통일도 겸사 해소).
- ③재질 교체 완료(Pretendard·쿨그레이+보라·그림자). 디자인 에이전트 제안 중 상태색 new=빨강만 채택 안 함 — 1라운드에서 "빨강=긴급 전용"으로 이미 축을 갈랐는데 되돌리는 셈이라 주황 유지. 다음 체크포인트 = ④이모지→SVG 아이콘 + 로그인 재구성, 그 뒤 ⑤새 눈 재검증.
- push(인각님 지시) — 폰 확인은 로컬 IP·방화벽보다 배포 데모가 간단해서. 이어서 ④ 완료: 이모지 10곳+를 인라인 SVG 매크로(_icons.html)로, 로그인은 명단 나열 대신 "이렇게 둘러보세요" 추천 3역할 카드(사장·강만석·박기사 — 각각 권한 분리의 증명 포인트를 한 줄로) + 전체 명단 접기. 노란 박스는 지시·특이사항 전용으로 좁히고 데모 공지는 중립 박스로 분리(점검 40번). 「이슈」→「클레임」 통일도 겸사(19번, 한 단어라). 남은 것 = ⑤새 눈 재검증.
- ⑤재검증 결과(수정 이력 모르는 새 리뷰어 5명 + 대조 판정): **고친 16건 전원 「해소」** — 같은 지적이 하나도 재등장 안 함. 새 지적 33건은 대부분 원래 있던 문제의 신규 발굴이지만 **회귀도 있었다**: 기사 「완료 처리」에 확인창 누락(내가 세운 "완료만은 묻는다" 원칙을 현황판에만 적용한 실수, sev3), 전역 링크색 미지정으로 기본 파랑 노출, 활성 메뉴 보라가 옅은 배경 위 대비 미달. 셋은 즉시 마감(한 줄짜리라 라운드로 미룰 이유 없음). 나머지는 2라운드 후보로 BATON에 — 배움: "만든 눈" 분리 검증이 실제로 회귀를 잡았다.
- 인각님 폰 확인 완료("어색한 곳 못 찾음") — UI/UX 단계 종료. 다음 = **②Postgres 이관** 착수. 첫 일 = 무료 조건 재확인(무료 조건은 수시로 바뀌므로 착수 시점 재검증이 원칙이었음).
- 무료 DB 재조사 결과: **Neon 채택** — 카드 등록 없음·만료 없음·0.5GB·5분 쉬면 잠들지만 접속 시 자동 기상(수백 ms). Supabase는 1주 미사용 시 일시정지에 수동/우회 기상이 필요해 "면접관이 불쑥 여는 데모"에 부적합. Render 자체 무료 DB는 30일 뒤 삭제라 애초 제외.
- 이관 설계 = **이중 지원**: DATABASE_URL 있으면 Postgres, 없으면 SQLite. 이유 — 로컬 개발의 간편함(파일 하나)을 버릴 이유가 없고, 갈림길을 얇은 껍데기 4개(PgRow·PgConn·insert_id·sql_avg_hours)에 가두면 본문 코드가 안 갈라진다. 갈린 지점 = 자리표(?/%s)·행 접근(Row/PgRow)·lastrowid(RETURNING id)·julianday(EPOCH)·DDL의 id 자동번호(IDENTITY)·executescript(문장 분할). 검증 = 빈 DB 재시드 + 서버 스모크(읽기 11화면·쓰기 3경로) SQLite 경로 통과. Postgres 실접속 검증은 Neon URL 받은 뒤.
- 한계 하나 기록: 사진 **파일**은 여전히 서버 디스크(비영속) — DB 줄은 남는데 재시작하면 업로드 파일만 사라져 깨진 이미지가 된다(데모 사진은 부팅 때 재생성이라 무관). 실데이터 단계에서 오브젝트 스토리지·DB 저장 중 택일 — v2 후보로.
- Neon URL 수령(싱가포르 리전) → 실접속 검증 3단계 전부 통과: ①표 생성+시드(수치 SQLite와 동일) ②서버 스모크(11화면+쓰기 3경로, 렌더 결과 바이트까지 동일) ③**영속성 — 서버 죽였다 다시 켜도 테스트 티켓 생존**(SQLite 시절엔 불가능하던 것, 이관의 존재 이유 증명). 검증 티켓은 데모 DB에서 삭제. 남은 것 = Render 환경변수 등록(인각님) — 코드 수정 없이 환경변수만으로 전환되는 구조가 여기서 값을 함.
- 트레이드오프 기록: 이관으로 "재시작=초기화"라는 데모 자동 리셋도 함께 사라짐 — 방문자가 남긴 시험 데이터가 쌓인다. 필요해지면 리셋 수단(Neon에서 표 비우기 또는 관리 기능)을 나중에 — 지금은 영속 증명이 우선이라 수용.
- 인각님이 Render 환경변수 등록 → 배포=Neon **양방향 검증 통과**: 배포 사이트에서 만든 티켓이 로컬→Neon 직접 접속에서 그대로 보임(번호 18부터가 증거 — 지운 17의 시퀀스 기억). **부수입: 배포 시각이 UTC로 찍히는 문제 발견**(04:19 — Render 서버가 UTC. 로컬(KST)에선 안 보이던 것) → now()를 KST 고정(+9, 서머타임 없어 안전)으로 수정. 경과일 계산도 같은 기준으로. 로그인 문구("재시작되면 초기화")·README(스택·로드맵)도 이관 완료에 맞게 갱신. KST 재검증까지 통과(13:22, 오차 0.6분) — **②이관 완전 종료**.
- 인각님 "다음으로 넘어가자" → **③카톡 알림** 착수. 첫 일 = 카카오 API 무료 범위(나에게 보내기) 현재 조건 재확인.
- 카카오 조건 확인: 「나에게 보내기」는 심사 없음(친구 발송만 심사) — 무료 범위 확정. 설계 결정 셋: ①모든 알림은 연동한 사장 본인 카톡으로(무료 API의 유일 범위를 데모 서사로 전환 — "접수되면 관리자 폰이 울린다") ②**토큰을 settings 표(Neon)에 보관** — 액세스 토큰이 수 시간짜리라 갱신 필요한데, 방금 만든 영속 DB의 첫 활용처. 리프레시 토큰 회전도 DB에 반영되어 재시작 무관 ③연동은 화면 속 OAuth(/kakao/setup → 동의 → /kakao/callback) — 토큰 복사·붙여넣기 없는 플로우.
- 안전장치 둘: 알림 실패는 접수를 못 막는다(try·무시 — 알림은 보조 축, 원 축은 대시보드·내 클레임). REST 키 등록 전엔 연동 입구 자체를 숨김 — 데모 방문자(사장 역할)가 설정 안내 문구를 만나면 안 되므로. 키 미설정 스모크(입구 숨김·접수 무해) 통과. 실발송 검증은 인각님 카카오 앱 생성 뒤.
- 연동 막힘 둘을 순서로 뚫음: ①KOE006 — Redirect URI 미등록(새 콘솔은 위치가 [앱]>[플랫폼 키]로 바뀜) ②KOE010 — **새 콘솔은 REST 키에 Client Secret이 기본 활성**이라 토큰 교환에 시크릿 필수. 500으로 죽던 콜백을 "카카오 에러 JSON을 화면에 그대로" 방식으로 고쳐서 ②를 원격 진단 가능하게 만든 게 주효. KAKAO_CLIENT_SECRET 환경변수 지원 추가로 해결.
- 인각님 지시: **UI 2라운드 1~10 진행** + 노트북 세션의 HANDOFF-ask-chat.md 확인. HANDOFF는 자기완결(아키텍처=노트북이 두뇌·Render가 얼굴, API 계약, 순서) — 데스크탑 몫(/ask 화면+프록시)을 병렬로 지금 구현하기로.
- 2라운드 반영: ①자동 갱신을 조작 인지형으로(입력 커서·최근 20초 터치 시 보류 + 접이 펼침 상태 sessionStorage 유지 — 벽모니터는 계속 갱신) ②이동 기록 kind='move' 신설(PG는 CHECK 제약 갈아끼우는 이관, 타임라인에서 위계 낮춤) ③데모 사진에 「시연용 예시 이미지」 딱지(깨진 이미지 오해 차단) ④폰 현황판 세로 스택 ⑤벽모니터 모드(/?tv=1 — zoom 확대+조작 UI 제거) ⑥로그인 카드 「들어가기 →」 ⑦건수 배지 전용색(b-count, 상태색 차용 해소)+「미완료 N건」 통일 ⑧ack→「지시 확인」·확인됨 노랑 어둡게 ⑨keep-all ⑩메뉴·로그아웃 터치 타깃. 겸사: 「칸반 보드」→「현황판」 통일.
- 함정 하나 기록: ④가 처음에 안 먹힘 — 미디어쿼리를 소스 **앞쪽** 블록에 넣어 뒤에 오는 .board{display:flex} 기본 규칙에 덮였다(같은 구체도는 소스 순서가 이긴다). 뒤 블록으로 옮겨 해결 — CSS 캐스케이드는 위치도 설계다.
- **세탁 상담 챗(데스크탑 몫) 구현**: /ask 화면(단발 질문-답, "세탁 공정과 얼룩" 문구 — 사서 방식은 설계임을 화면이 말함) + /api/ask 프록시(로그인·2~500자 검증, 타임아웃 120초, 실패 시 "상담원이 쉬고 있습니다") + ask_log 표(질문이 다음 개선의 나침반 — HANDOFF 권장). 답의 ※·⛔ 딱지는 textContent로만 그려 가공·주입 둘 다 차단. BRAIN_URL 없으면 메뉴 자체가 숨음 — 노트북 작업과 완전 독립 배포 가능.
- **작업 분업 지침 수신(인각님)**: 단순 코딩은 싼 모델(서브에이전트)에게, 계획·검수는 Fable이. 메모리에 저장 — 다음 작업부터 적용.
- 인각님 피드백: 상담 화면이 "AI 챗처럼 안 보인다" → **LLM 챗 스타일로 재작성**. 분업 지침 첫 적용 — 스펙(말풍선·타이핑 점·알약 입력줄·Enter/IME 처리·textContent 불변식)을 상세히 써서 sonnet에 위임, 검수에서 **버그 1건 적발**(Jinja 매크로 출력을 JS 문자열에 직접 넣어 줄바꿈으로 문자열이 끊김 — DOM 복제 방식으로 수정). 위임+검수 체계가 첫 판부터 값을 함.
- 노트북 터널 주소 수신(임시) → **종단 시험**: 지식 질문 7.9초 정상(근거·딱지 표시 확인). **발견 2건** — ①"박기사 오늘 할 일" 같은 업무 질문에 거절 대신 녹 얼룩 지식을 끌어다 지어냄(사서 계약 위반 유형) ②"꿉꿉한 냄새"가 곰팡이 지식에 연결 안 되고 거절됨(일상어 매칭). 인각님의 "오늘 할 일 답변 가능?" 질문의 답 = **context 계약 확장으로 가능**(사이트가 배정 티켓을 딸려 보내고 모델은 그것만 근거로) — 발견① 가드와 한 몸으로 설계해야 함. 셋 다 HANDOFF-ask-chat-r2.md로 노트북 세션에 회신.
- Render 환경변수는 미등록 유지 — 임시 터널 주소(재시작마다 바뀜)라 상시 구성 뒤가 맞다.
- 인각님 요청: **대화내역 저장 + 방 여러 개(주제별) + 이미지 입출력 + "챗 제품의 표준이 뭐냐"**. 사이트만으로 되는 것(방·이력)과 모델이 필요한 것(기억·이미지)을 갈라, 앞엣것만 이번에 구현.
- 멀티세션 구현(분업 2번째): 스키마·라우트 설계는 내가(chats/chat_msgs 두 표, 방은 첫 질문 때 생성 — 빈 방 방지, 질문 선저장 — 두뇌가 죽어도 질문은 남게, 소유 확인 2중), 코딩은 sonnet 위임. 검수 통과(이번엔 버그 0 — 스펙에 함정을 미리 적어준 효과로 보임). ask_log는 chat_msgs로 대체하며 정리. 종단 시험: 방 2개 생성·후속 질문·목록·재열람 영속·남의 방 차단(redirect+403) 전부 통과.
- 노트북 r3 회신 수신: 발견① 결정적 가드로 봉쇄(90문 회귀 바이트 동일 — 오발동 0 증명 방식이 좋다), 발견②+context 확장은 합격선 재등록 배치로 묶음(인각님 결정 대기), 상시 터널은 Cloudflare 계정 답 대기. 릴레이(r1→r2→r3)가 git 문서로 도는 구조가 자리잡음.
- 인각님 질문 셋에 답: ①"모델이 DB를 알아야 하지 않나" — 맞는 직관. 단 모델이 DB를 통째 아는 게 아니라 **질문 순간에 그 사람 몫 조각만 딸려 보내는** 구조(context 주입)가 정답 — 권한(남의 티켓 차단)·신선도·크기 모두 이쪽이 맞다 ②"왜 피는 과산화수소?" 실증 — 거절 대신 반쯤 조립된 답이 나왔고 **같은 모델의 두 답이 철분 처리를 놓고 모순**(원리 지식 얕음이 원인) ③"검색해서 알려줄 수 있나" — 기술적으론 되지만 사서 원칙·안전 가드가 무너져 반대. 대안 = **검색→감수→지식 추가 파이프라인**(거절 로그가 수집 나침반).
- r4 작성·push: A(context에 대화 이력까지 실어 멀티턴 기억+업무 답변 한 계약) → B(스트리밍) → C(이미지 입력) → D(원리 지식+검색 취급 — 인각님 결정 항목 3개 명시). 실증(철분 모순)을 D의 첫 감수 후보로 넘김.
- 인각님이 「LLM 디자인 예시.PNG」(Gemini 캡처)를 두고 "목록이 블록이 아니라 옆에 쭈르륵"을 요청 → **데스크톱은 대화 화면 안에 Gemini식 사이드바**(+새 상담 알약, 최근 목록, 현재 방 하이라이트), **폰은 공간이 없어 기존 목록 화면 유지**(사이드바 CSS 숨김 + ← 링크)로 갈랐다 — 앱 안에 앱(전역 내비 + 챗 사이드바) 이중 구조라 폰까지 밀어넣으면 오히려 예시와 멀어진다는 판단. 코딩 sonnet 위임(분업 3번째, 버그 0), 데스크톱·폰 캡처로 검수.
- 인각님 후속 지적 셋(이름·사이드바 중앙 떠 있음·입력창 위에 떠 있음) 반영: 「세탁 상담」→**「AI 채팅」**(인각님이 쓰던 말), 이 화면만 전폭+대화 세로 flex(입력창 바닥). 검수에서 **캐스케이드 함정 재적발** — 화면 전용 style이 전역보다 소스 앞이라 같은 구체도 main{}이 전역에 져서, 직전의 1160px도 사실 미적용이었음(사이드바가 중앙에 뜬 진짜 원인). .content main으로 교정. 같은 함정 두 번째(현황판 세로 스택 때와 동일) — ask.html 주석에 경위를 박아둠.
- 시험 중 터널 530(노트북 터널 사망) 확인 — 오류가 말풍선으로 곱게 표시되는 것까지 실검증됨. 터널은 임시라던 노트북 말대로 — 상시 구성(Cloudflare 계정) 결정이 더 급해짐.
- "알림이 안 와" 진단: 발송 API는 result_code 0(성공)이었다 — 원인은 도착 위치. 나에게 보내기는 **「나와의 채팅」방에 쌓이고 푸시가 안 울린다**(카톡 정책). 인각님이 그 방에서 메시지 확인. 겸사 인각님 지적(누구 계정인지·해제 수단 없음) 반영: 연동 계정 표시(끝 4자리)·테스트 발송·해제(unlink+토큰 삭제)·재연동은 prompt=login으로 계정 전환 가능하게. 종단 검증 = 배포 접수 → 카톡 도착.

