# 2026-08-10 · laundry-ticket

> 이 날의 작업 기록. **"왜"만 적는다** — "무엇이 바뀌었나"는 git 이력에 있다.
> 아직 안 끝난 것은 `..\BATON.md`에. **한번 적으면 안 고친다.**

## laundry-ticket

- 세션 시작, 오늘 할 일 정리 요청. BATON 기준 남은 일 셋(이력서 링크 반영·GitHub About 란·README 캡처) 확인. 추천 순서 = About 란 → 캡처, 이력서 반영은 라이넨스 지원 세션과 겹치므로 그쪽으로 미루는 쪽 제안 — 이유: About 란은 혼자 5분에 끝나고, 이력서는 프로젝트 하나가 아니라 이력서 전체를 만지는 일이라 이 세션 범위를 넘는다. 아직 인각님 선택 대기 중.
- 방향 전환: 남은 잔일 대신 **v2 착수** 결정(인각님). 기존 기능 목록을 코드에서 뽑아 정리하고, v2 후보 셋 중 **Postgres 이관 우선**을 제안 — 이유: 셋 중 이력서 경험 카드가 되는 건 이관뿐이고, 인증은 데이터가 영속해야 실익이 생기며, SMS는 유료·발신번호 등록이라는 외부 절차에 걸린다. 인각님 선택 대기.
- v2 방향 확정(인각님): **①UI/UX 점검·디자인 업그레이드 먼저 ②DB는 무료 Postgres(카드 등록 없는 서비스만 — 과금 원천 차단) ③알림은 SMS 대신 카톡, 무료 범위만**. UI/UX는 "만든 눈"이 못 보는 어색함이 걱정이라 검증 분리 요청 — 방식 = 폰 폭 스크린샷 + 수정 이력을 모르는 다중 리뷰어 병렬 점검, 겹치는 지적을 진짜 문제로 채택, 고친 뒤 새 눈으로 재검증.
- UI/UX 점검 완료(리뷰어 5 + 디자인 방향 1, 병렬·상호 비공개): 원지적 65건→병합 41건. 최다 겹침(4명) = 폰 상단 내비 잘림으로 「내 클레임」 발견 불가 — 폰 우선 원칙이 내비에서 무너져 있었다는 뜻. 디자인 진단 = "뼈대는 이미 플로우와 같음, 표면 셋(이모지·맑은고딕·초록 톤)이 인상을 깎음 → 구조 유지·재질 교체". 첫 라운드 12건 + 재질 교체 순서로 로드맵을 보고서(artifact)로 발행, 인각님 범위 선택 대기.
- 인각님 선택 = "기반+12건부터(단계마다 확인)". 반영 완료 — CSS 색 토큰화 + 12건. 결정 둘: ①확인 대기 색은 회색이 아니라 **주황** — "아직 아무도 안 본 것"이 눈에서 물러나면 안 되고, 빨강만 긴급에 반납하면 축 분리는 충분. ②상태명 「접수」→「확인 대기」는 화면 표시만 — DB 코드(new)와 CLAUDE.md의 상태 흐름 표기는 안 건드림(규칙 파일은 선설명·후승인 원칙). 1차 검증 = 로그인 리다이렉트 3역할 기능 테스트 + 동일 조건 스크린샷 14장 재촬영. 새 눈 재검증은 재질 교체 뒤 한 번에.
- 전후 비교는 두 버전 동시 기동(수정 전 = git 이력에서 워크트리로 꺼내 8766)으로 제공 — 캡처만으론 "직접 눌러보는" 확인이 안 되므로. 인각님 PC 확인 후 피드백 1건: **담당자별 클레임에 담당자마다 미완료 건수를 숫자로** → 즉시 반영(미완료 N건·긴급 N 배지 + 공장별 그룹핑, 점검 15번 지적의 그룹핑 통일도 겸사 해소).
- ③재질 교체 완료(Pretendard·쿨그레이+보라·그림자). 디자인 에이전트 제안 중 상태색 new=빨강만 채택 안 함 — 1라운드에서 "빨강=긴급 전용"으로 이미 축을 갈랐는데 되돌리는 셈이라 주황 유지. 다음 체크포인트 = ④이모지→SVG 아이콘 + 로그인 재구성, 그 뒤 ⑤새 눈 재검증.
- push(인각님 지시) — 폰 확인은 로컬 IP·방화벽보다 배포 데모가 간단해서. 이어서 ④ 완료: 이모지 10곳+를 인라인 SVG 매크로(_icons.html)로, 로그인은 명단 나열 대신 "이렇게 둘러보세요" 추천 3역할 카드(사장·강만석·박기사 — 각각 권한 분리의 증명 포인트를 한 줄로) + 전체 명단 접기. 노란 박스는 지시·특이사항 전용으로 좁히고 데모 공지는 중립 박스로 분리(점검 40번). 「이슈」→「클레임」 통일도 겸사(19번, 한 단어라). 남은 것 = ⑤새 눈 재검증.
- ⑤재검증 결과(수정 이력 모르는 새 리뷰어 5명 + 대조 판정): **고친 16건 전원 「해소」** — 같은 지적이 하나도 재등장 안 함. 새 지적 33건은 대부분 원래 있던 문제의 신규 발굴이지만 **회귀도 있었다**: 기사 「완료 처리」에 확인창 누락(내가 세운 "완료만은 묻는다" 원칙을 현황판에만 적용한 실수, sev3), 전역 링크색 미지정으로 기본 파랑 노출, 활성 메뉴 보라가 옅은 배경 위 대비 미달. 셋은 즉시 마감(한 줄짜리라 라운드로 미룰 이유 없음). 나머지는 2라운드 후보로 BATON에 — 배움: "만든 눈" 분리 검증이 실제로 회귀를 잡았다.
- 인각님 폰 확인 완료("어색한 곳 못 찾음") — UI/UX 단계 종료. 다음 = **②Postgres 이관** 착수. 첫 일 = 무료 조건 재확인(무료 조건은 수시로 바뀌므로 착수 시점 재검증이 원칙이었음).
- 무료 DB 재조사 결과: **Neon 채택** — 카드 등록 없음·만료 없음·0.5GB·5분 쉬면 잠들지만 접속 시 자동 기상(수백 ms). Supabase는 1주 미사용 시 일시정지에 수동/우회 기상이 필요해 "면접관이 불쑥 여는 데모"에 부적합. Render 자체 무료 DB는 30일 뒤 삭제라 애초 제외.
- 이관 설계 = **이중 지원**: DATABASE_URL 있으면 Postgres, 없으면 SQLite. 이유 — 로컬 개발의 간편함(파일 하나)을 버릴 이유가 없고, 갈림길을 얇은 껍데기 4개(PgRow·PgConn·insert_id·sql_avg_hours)에 가두면 본문 코드가 안 갈라진다. 갈린 지점 = 자리표(?/%s)·행 접근(Row/PgRow)·lastrowid(RETURNING id)·julianday(EPOCH)·DDL의 id 자동번호(IDENTITY)·executescript(문장 분할). 검증 = 빈 DB 재시드 + 서버 스모크(읽기 11화면·쓰기 3경로) SQLite 경로 통과. Postgres 실접속 검증은 Neon URL 받은 뒤.
- 한계 하나 기록: 사진 **파일**은 여전히 서버 디스크(비영속) — DB 줄은 남는데 재시작하면 업로드 파일만 사라져 깨진 이미지가 된다(데모 사진은 부팅 때 재생성이라 무관). 실데이터 단계에서 오브젝트 스토리지·DB 저장 중 택일 — v2 후보로.
- Neon URL 수령(싱가포르 리전) → 실접속 검증 3단계 전부 통과: ①표 생성+시드(수치 SQLite와 동일) ②서버 스모크(11화면+쓰기 3경로, 렌더 결과 바이트까지 동일) ③**영속성 — 서버 죽였다 다시 켜도 테스트 티켓 생존**(SQLite 시절엔 불가능하던 것, 이관의 존재 이유 증명). 검증 티켓은 데모 DB에서 삭제. 남은 것 = Render 환경변수 등록(인각님) — 코드 수정 없이 환경변수만으로 전환되는 구조가 여기서 값을 함.
- 트레이드오프 기록: 이관으로 "재시작=초기화"라는 데모 자동 리셋도 함께 사라짐 — 방문자가 남긴 시험 데이터가 쌓인다. 필요해지면 리셋 수단(Neon에서 표 비우기 또는 관리 기능)을 나중에 — 지금은 영속 증명이 우선이라 수용.

