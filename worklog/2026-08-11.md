# 2026-08-11 · laundry-ticket

> 이 날의 작업 기록. **"왜"만 적는다** — "무엇이 바뀌었나"는 git 이력에 있다.
> 아직 안 끝난 것은 `..\BATON.md`에. **한번 적으면 안 고친다.**

## 아침 — r7 대기 중 병행 후보부터 (원격 세션)

- 노트북 r7 회신 아직 없음(pull 결과 새 커밋 0) → BATON 병행 후보 중 **내가 원격으로 끝낼 수 있는 것**부터: GitHub About + README.
- **About은 gh CLI가 이 컴퓨터에 없어서** git 자격증명 관리자의 토큰으로 GitHub API를 직접 호출해 등록(설명·홈페이지·토픽 7개). 첫 PATCH가 조용히 실패(셸 인라인 JSON의 한글 인코딩) → JSON을 파일로 빼서 재시도, 성공 확인.
- README에 「AI 채팅 — 서버 밖의 두뇌」 절 신설. 왜: 지금 README에 챗 이야기가 전혀 없는데, 이 프로젝트에서 팔 것의 절반이 노트북 두뇌 아키텍처(사서 방식·합격선 시험·업무 질문)다. 터널 주소·토큰은 적지 않음(공개 저장소). 화면 캡처는 인각님 브라우저 몫으로 남김(BATON 유지).

## 오전 — 데모 데이터 리셋 수단 (인각님 선택)

- 다음 작업을 물었더니 리셋 수단 선택. 설계 결정 셋과 이유:
  - **settings 표는 안 지운다** — 카카오 토큰이 거기 있다. 리셋의 일은 방문자 흔적 정리지 연동 해제가 아니다(해제는 /kakao/disconnect 몫).
  - **Postgres는 TRUNCATE … RESTART IDENTITY CASCADE 한 문장** — 시드의 add_ticket이 "거래처 3번"처럼 자동 id 순서에 기대므로, id를 1부터 다시 매기지 않으면 재시드 배정이 전부 어긋난다(그냥 DELETE면 Neon에서 FK 위반으로 터졌을 것). staff id도 같은 순서로 재생성돼 로그인 쿠키가 리셋을 견딘다.
  - **JS confirm 대신 확인 페이지를 관문으로** — 지워질 규모(클레임·조치·대화 건수)를 숫자로 보여주고 누르게 한다. 사장 role 가드는 GET·POST 양쪽에.
- 분업: 스펙은 내가, 구현은 sonnet, 검수·시험은 내가. 이번 검수에서 구현 버그는 없었고, 오히려 내 시험 스크립트의 기대값(시드 티켓 15건)이 틀려 있었다(실제 16건) — 검수자도 검증당한 셈.
- 시험은 TestClient 종단 9항목(권한 가드·복원 건수·settings 보존·업로드 정리·쿠키 생존) 전부 통과 + 화면 3장 육안 확인. **실배포 리셋 버튼은 내가 안 누른다** — Neon에 인각님의 상담 대화방이 있어서, 실제 실행은 인각님 판단으로.

## 오후 — r7 검토 → r8 발신 (이미지 계약 합의)

- r7 도착. r6의 내 추정("범위 가드가 history 반영 전 발동")은 노트북 로그로 반증됨 — 조립도 배관도 무죄, 모델 자신이 소심했던 것. 추정을 틀렸다고 확인해 준 회신이 온 건 릴레이가 일하고 있다는 뜻이라 그대로 기록.
- 실배포 재검증: r6 실패 사례 원문 재현 → 정답 전환 확인(조언11), 시험 방 4는 Neon에서 삭제.
- 이미지 1단계 계약은 합의하되 r8에 명확화 4건을 실어 보냄. 핵심 둘:
  - **전송 형식 JPEG 고정** — 프록시가 어차피 재인코딩하므로 노트북에 PNG/WebP 분기를 만들게 할 이유가 없다.
  - **EXIF 실측** — Pillow 재인코딩이 메타데이터를 전부 지우는 건 확인했는데, 회전 태그까지 같이 지워져 **exif_transpose를 먼저 안 하면 폰 세로 사진이 누워 저장된다**는 함정을 구현 전에 찾았다. 노트북이 확인해 달라던 숙제를 실측으로 답한 것.
- 순서는 r7 제안 그대로(노트북 수신부 먼저). 데스크탑은 그동안 화면·축소부를 미리 만들어 에코로 자체 검증하기로.

## 오후 — 사진 첨부 데스크탑 몫 선행 구현 (인각님 "시작")

- 게이트 설계: **환경변수 BRAIN_IMG가 켜져야만** 사진 버튼 노출 + payload에 image 포함. 왜: 노트북 수신부가 없는 지금 배포에 실리면 두뇌가 모르는 필드를 받게 된다 — Render에 변수를 안 넣는 한 배포해도 기존 동작과 완전히 같다(회귀 0). r9 오면 변수 하나로 켠다.
- 분업 3회째: 스펙(r8 계약 + 가드 조건 명시) → sonnet 구현 → 내 검수. 이번 검수 수확 둘:
  - **실버그**: FastAPI가 multipart의 빈 필드를 「누락」으로 취급해 사진만 보내기(글 없음)가 422로 죽는다 — 에코 종단 시험에서 발견, question 기본값을 ""로 바꿔 해결. 브라우저 FormData도 똑같이 보내므로 화면에서도 터졌을 버그.
  - 잔손질: 사진만 보낼 때 말풍선에도 "(사진만 보냄)" 문구를 띄워 새로고침 전후가 같은 대화로 읽히게.
- 검증: 에코 두뇌 종단 5항목(JPEG 고정·1600px·회전 보정·EXIF 0건·대체 문구·history 무오염·비이미지 거절) + 플래그 끔 회귀(사진 UI 부재, 글 질문 정상) + 화면 3장(첨부 미리보기·데스크톱·모바일 사진만). r9 후 실배포 종단만 남음.

## 저녁 — r9 수신 → BRAIN_IMG 점화 → 실배포 종단 → r10 발신

- r9 도착(노트북 수신부 완료). 인각님이 Render에 BRAIN_IMG=1 등록 → 2분 만에 사진 버튼 실배포 노출.
- 종단 3종(사진+글/사진만/글만) 모두 형식 통과 — 특히 「사진만」은 노트북이 LLM을 안 부르고 고지만 즉답하는 r9 설계가 실배포에서 그대로 동작.
- **회귀 1건 발견**: 응답 JSON의 sources가 빈 배열로 옴(본문 딱지는 정상 → 화면 근거 칩만 죽음). 이미지와 무관한 글만 경로에서도 재현 + 오전(이미지 배포 전) 정상이던 시점 근거까지 잡아 r10에 보고 — 노트북 이미지 업데이트의 응답 조립 회귀로 추정. 종단 시험을 오전·오후 두 번 돌린 덕에 "언제부터 깨졌나"를 말할 수 있었다.
- 시험 방 5·6은 Neon에서 삭제. 폰 실기기 촬영 확인은 인각님 몫으로 r10에 남김.

## 저녁 — r11 확인 → 칩 분기 → r12 완료 선언

- r11의 반전: sources는 회귀가 아니라 **원래 있던 구멍**(얼룩 인용은 처음부터 sources에 안 실림). 내 r10 추정이 또 한 번 로그로 반증됐다 — 오전에 "정상"으로 본 사례는 조언 인용이라 유형이 달랐던 것. 시점 증거는 유형까지 같아야 증거다.
- r11 §1의 ⚠(얼룩 행엔 n이 없다)를 확인하니 정확히 렌더러 두 곳(JS·Jinja)이 n에 기대고 있었다 — 「근거 undefined」가 뜰 상태. 두 줄 분기라 위임 없이 직접 수정(분업 기준: 스펙을 쓰는 시간이 고치는 시간보다 길다).
- 실배포 검증: 얼룩 칩·조언 칩·새로고침 렌더 전부 정상, 이상 칩 0건. 시험 방 7 삭제. r12로 **이미지 1단계 완료 선언** — r7→r12 여섯 판 릴레이로 계약 제안부터 개통·검증까지 하루에 끝났다.

## 밤 — 인각님 폰 실사용이 기계 시험이 못 잡은 것을 잡다 (r13)

- 첫 증상 「상담원이 쉬고 있습니다」는 노트북 꺼짐(글만 질문도 0.5초 즉사로 판별). BATON 주의에 시연 체크리스트 추가.
- 노트북 부활 후 진짜 발견: 깨끗한 에코백 사진에 "이 사진은 무엇인지 너의 생각을 말해봐" → 모델이 **"이 사진은 선크림 얼룩입니다"** — 볼 수 없는 사진을 봤다고 지어냄(근거 딱지까지 달고). 판독하는 척 금지(r7 원칙) 위반이라 r13으로 즉시 발신.
- 왜 기계 시험이 못 잡았나: 내 종단 시험 질문은 전부 "정상 사용자" 모양이었다 — "사진을 가리키며 묻는" 자연스러운 사용을 안 던져봤다. 실기기 확인을 인각님 몫으로 남긴 게 맞았다.
- 문구 결정(인각님): 봇 답변에서 "받아뒀다"(저장 언급)를 뺀다 → 고지가 사라지면 몰래 수집이 되므로 **수집 고지를 첨부 시점 화면 안내로 이동**(보내기 전에 보이니 고지로서 오히려 강함). 데스크탑 반영, 노트북 몫은 r13 보강으로.
- 2단계 범위 확정(인각님): VLM은 보는 것만, 그중에서도 **품목 12종 분류만**("수건으로 보입니다") — 얼룩 판독은 제외. 왜 이 축소가 좋은 결정인가: 닫힌 목록 분류는 정답 라벨 시험이 가능해서, 인각님이 사진 세트를 못 만들어도 합격선을 세울 수 있다. 오늘 에코백 사건의 교훈("목록에 없다"/"모르겠음" 출구가 핵심)이 그대로 합격선 축이 된다.
- r14 도착(사칭 수정 3겹 + 합격선 v5 108문 — 사진 사칭 축 신설, 전 문항 전수 검사) → 실배포 재확인 4종 + 화면 2장 전부 합격, r15로 종결 회신. 사칭 발견(폰 실사용)부터 수정·합격선 박제·재검증까지 **당일 안에 릴레이 세 판**으로 닫았다. 남은 릴레이는 2단계 계약 초안(노트북) 하나.
- README 캡처를 내가 마무리("인각님 브라우저" 조건을 변경) — 인각님이 원격이라 캡처를 못 주고, 배포 데모를 Playwright로 찍어도 같은 화면이며 전부 가상 데이터다. 단 대시보드 캡처에 **카카오 계정 끝자리가 찍혀** 그 줄만 화면에서 숨기고 재촬영(공개 저장소 개인정보 원칙). 캡처용 채팅 방은 삭제, README에 사진 첨부 기능 한 줄도 추가.

## 밤 2부 — UI/UX 3라운드+ (인각님 "편의성·가독성 최적화해줘")

- 어제 방식 재사용: 전 화면 26장 캡처 → 블라인드 검토자 3(과업 편의성/가독성·위계/모바일·접근성) 병렬 → 발견 23건 병합 → 21건 확정. BATON의 잔여 후보 4건(공장 접기·/board 고아·사진 버튼·색약)이 전부 독립 검토에서 재발견·구체화됨 — 남겨둔 목록이 맞았다는 교차 확인.
- 눈에 띄는 발견: ①ticket.html 조치 폼의 종류 select 기본값(지시)이 append-only 기록에 영구 오기록 사고를 만든다(기사용 me.html은 이미 버튼식인데 관리자 화면만 낡음) ②접수 폼이 거래처↔담당자 공장을 연동하지 않아 다른 공장 직원 배정이 기본 상태 ③모바일 메뉴가 44px 기준 미달(칸반 버튼은 맞춰놓고 진입 메뉴가 빠짐).
- 구현은 sonnet 위임(21건 스펙), 검수·재캡처는 내가.
- 검수 결과: 21건 전부 스펙대로, 구현 버그 0. 눈으로 확인한 큰 변화 — 공장 상세 2,450px→한 화면(접기), 담당자별 클레임이 "일 가진 사람 먼저"로 재정렬+강조, 티켓 조치 폼이 종류별 버튼으로(오기록 사고 원천 차단), 접수 폼 공장 연동(JS+서버 이중), 고아 /board는 리다이렉트로. 동작 3종(303/400/303) 스모크 통과. 구현자(sonnet)와 검증자(나)를 가른 것은 유지 — 어제 원칙.
- 소소한 함정 재발 2건: PowerShell -replace가 스크립트 속 한글을 깨뜨림(스크립트는 Write 도구로 직접 쓸 것), curl -d의 한글이 cp949로 들어가 DB 조회가 어긋남(시험 데이터는 id로 지울 것).

