# 2026-08-13 · laundry-ticket

> 이 날의 작업 기록. **"왜"만 적는다** — "무엇이 바뀌었나"는 git 이력에 있다.
> 아직 안 끝난 것은 `..\BATON.md`에. **한번 적으면 안 고친다.**

## 낮 — r23 수신(v7 배포) 검증 → r24 발신
- 검증 방법: Neon을 직접 조회해 정답지(미완료 긴급 4건 = 1·4·14·17)를 만든 뒤 실서버 새 방에서 질문 — 시점 증거는 정답지부터 (r10 교훈의 실천).
- 긴급 질문: 정확히 4건만, 이름 전부 정확, 비긴급 혼입 0 — G03b(r18에서 내가 잡은 결함) 실서버 소멸 확인. 8-12 이후 새로 생긴 티켓 17까지 정확히 센 것 = 렌더러 결정적 카운트 방식의 장점 실증.
- 혈액 삶기 질문: "안 됩니다(단백질 고착)"로 교정 확인.
- 정정 접수: 내 r22 "404=노트북 잠듦"은 틀린 추정을 사실처럼 쓴 것 — 실제는 터널 인증 사고(노트북 샌드박스 authtoken). 교훈: 원인을 확인 못 했으면 "확인 못 함"이라 쓴다 (8-12 README 건과 같은 결).
- 시험 방 9 삭제, 인각님 방 1·8 보존.

## 저녁 — 판독 능력 자체 시험 (인각님 지시: "얼마나 판독하는지 검증해봐")
- 방법: 위키미디어 공용(CC)에서 실사진을 받아 **내가 눈으로 정답을 먼저 확정**(부적격 6장 교체 — 종이 냅킨·원경·공예품 등, r21의 세트 재검수와 같은 기준을 나도 밟았다) → 12장(품목 9+목록에없음 3)을 실서버 사진만 턴으로 전송.
- 결과: 오단정 0(핵심 축 유지) · confident 6/6 전부 정답권 · 목록에없음 출구 2/2 · **후퇴 4장(33%, 전부 60~62s 상한)**. 어려운 유형 = 혼재·클로즈업·복잡 배경 착용컷·인물.
- 내 채점기 버그 하나: 후퇴 문구를 "준비 중"으로만 찾아 4건을 오판독으로 잘못 집계 — 실제 문구는 "지원하지 않습니다"였다. 사람 눈으로 재채점해 정정. 교훈: 문구 대조는 실제 배포 문구로.
- r25 발신: 결과 + 수집함 고지(오늘 유입 12장 = 시험용 CC, 내 정답 라벨 첨부 — 얇은 품목 표본으로 재활용 가능) + 후퇴 문구 재고 관찰("지원하지 않습니다"는 판독 성공 경험과 충돌하기 시작). 시험 방 10 삭제.

## 밤 — r26 수신(후퇴 문구 당일 교체 배포·교차 검증) → r27 발신(대기 한도 답)
- r26: 내 12장 시험을 ask_log로 교차 검증(일치. 시트 2건은 내부 guess — 화면 문구가 단일이라 밖에선 구분 안 됨), 내 §2 관찰을 당일 수용해 후퇴 문구 교체 배포("이번 사진은 판독하지 못했습니다"), 인각님 폰 재시도도 이미 완료(에코백 ✓ — BATON에서 내림). 문의 1건 = 판독 예산 90s 인상 전에 사슬 대기 한도.
- r27 답: 프록시 120s(코드)·브라우저 무제한(fetch 타임아웃 없음)은 확실한데 **게이트웨이는 공식 문서에 명시가 없다**(재검색). 통설 ~100s를 근거로 예산을 정하는 건 추측이라, 노트북이 시험 전용 지연 필드를 넣으면 내가 70~115s를 프로브해 실제 한도를 확정하자고 제안. 사진+글 턴의 순차 합산 문제도 확인 항목으로 얹음.

## 밤 2 — 게이트웨이 실측(r28→r29): 통설이 틀렸다, 병목은 우리 프록시
- r28(debug_delay 훅 준비) 수신 후 깨달은 내 설계 결함: 그 필드는 사이트 경유로는 노트북에 못 닿는다(프록시가 payload를 조립). 방법 전환 — 앱에 임시 계측 라우트(사장 전용 N초 sleep)를 얹어 게이트웨이 구간만 직접 쟀다.
- 결과: 70~125s 전부 완주, 115·125s HTTP 200 확정. **통설 ~100s는 이 서비스에 해당 없음 — 실제 병목은 우리 프록시 120s(조정 가능한 우리 설정).** 첫 5발이 상태코드를 못 찍은 건 출력 경로 문제(Git Bash /tmp 쓰기 실패)였고 재발로 확정.
- 임시 라우트는 실측 직후 제거(추가·제거 커밋 2개 — 계측 코드를 본체에 안 남긴다). r29 발신: 결과 + 남은 미지수는 터널 구간뿐(노트북이 자기 URL로 1분 프로브) + 판독 예산 90s GO 권고(수치는 인각님 등록).

## 밤 3 — r30 수신: 사슬 닫힘, 예산 90/110 배포. 실측 트랙 종결(r31)
- 터널 ≥110s 통과 → 인각님이 r29 권고 그대로 등록(판독 90s·사슬 110s·턴별 차등 자동식) → 훅 제거까지 하루에 완결. r25의 후퇴 4장 재판독 = 2장 구제(후퇴율 33%→17%, 오단정 여전히 0) — 내 12장 세트가 예산 결정의 근거 데이터가 됐다.
- 내 스모크 1발(가운 실사진 36.8s confident 정답)로 배포 후 회귀 없음 확인, r31 발신으로 트랙 마감. 사슬 실측치는 BATON 주의에 박제 — 나중에 다시 재지 않도록.
