# 2026-08-07 (laundry-ticket)

- **🚀 배포 완료 — https://laundry-claim.onrender.com (Render 무료 티어, GitHub 연동).** 원격 = github.com/IngakHwang/laundry-claim (공개). 이로써 라이넨스 자격요건 「API·DB·클라우드로 서비스 구축」이 링크로 닫힘. 배포 검증 = 로그인·대시보드·예시 데이터·데모 사진 자동 생성까지 원격 실측 통과.
- 공개 푸시 전 **커밋 역사 세탁**: flow 캡처(타사 관련 문구 노출)와 옛 DB 백업을 git-filter-repo로 역사에서 제거 — 공개 저장소에 남의 정보가 실리면 안 되므로. 피드백 커밋 이력 21개는 보존(그 이력 자체가 포트폴리오라서).
- Render 선택 근거: 공식 문서 직접 확인(무료 웹 서비스 가능·월 750시간·카드 불요·디스크 비영속). 경쟁사 비교 페이지의 "무료는 정적만" 주장은 원문 확인 결과 거짓 — 스니펫 불신 규칙이 일함.

- 프로젝트 시작. 왜: 라이넨스 대비 마지막 빈칸 「API·DB·클라우드 서비스 구축」을 채울 새 물건 — 후보 셋(접수 장부 / 기사 인수인계 / 합치기)을 거쳐 **인각님이 직접 컴플레인 추적으로 설계를 확정**했다(기록→배정·전달→조치→완료). 접수 장부와 인수인계는 이 구조의 부분집합으로 흡수됨.
- 설계 문답 없이 바로 v1을 만들기로 함. 왜: 인각님 지침 "만든 것 보고 추가하거나 뺄 것 있으면 빼자" — 글 설계보다 작동물에 반응하는 방식([[나는 글이 아니라 작동하는 결과로 검증한다]]).
- v1을 보자마자 인각님이 첫 추가 요청: **사진 첨부** — "어떤 시트의 이 부분에 오점이 반복된다"를 말이 아니라 사진으로. 접수와 조치 보고 양쪽에 붙임(폰 카메라 직접 촬영). 이 물건의 존재 이유(전화·문자가 못 하는 것)와 같은 결이라 즉시 수용. 실측 검증(업로드→저장→화면 표시) 통과.
- 검증 중 발견: 인각님이 이미 티켓 #3으로 접수→지시→완료 한 바퀴를 직접 돌려봄 — v1이 첫 실사용자를 얻은 순간.
- **전문가 검수 1호 — 인각님이 품목 수량을 정정.** Claude 추정(객실당 시트 2장)이 틀렸고 현장 기준은 시트 1·커버 1·베개피 2~4·대타올 1~2·중타올 3~4·발매트 1. 품목 이름도 현장 용어(대타올·중타올·발매트)로 교체. 이 데이터의 언어와 숫자는 인각님 것이어야 한다는 원칙이 실제로 작동한 첫 사례.
- 피드백 7호: 한 페이지 나열 대신 flow식 왼쪽 사이드바 골격으로 전환(1차). flow 실물 캡처를 받으면 그 모양에 맞춰 다듬기로 함.
- 피드백 10호: **칸반 보드.** "지라·노션처럼 시각적으로"의 정체는 칸반(상태별 열 + 카드 + 끌어놓기) — 업계 표준 패턴이라 검색 없이 확정. 카드 끌어서 열에 놓으면 상태가 바뀌고, **그 이동도 조치 일지에 자동으로 남는다**(추적 원칙과 시각 편의가 충돌하지 않게). 카드에 긴급 띠·담당·경과일("N일째")·사진 개수. 역할 범위도 그대로 적용(2공장 인력 보드엔 2공장 카드만 — 실측).
- 피드백 9호: **역할별 화면 범위(인가).** 인각님 요구 = 내 티켓은 본인 것만 / 공장 사람은 자기 공장 이슈·거래처만 / 본사·사장은 전체. **인증(본인 증명)과 인가(무엇을 보나)를 분리**해 인가는 전 화면에 제대로 구현하고, 인증은 간이 로그인(이름 선택→쿠키, 비밀번호 없음)으로 — 데모 목적엔 역할 체험이 핵심이고 비밀번호는 공개 배포 때 판단. 부수 효과: 조치 기록의 「누가」가 폼 선택이 아니라 로그인 기준이 됨(남 이름으로 기록 불가). v1 봉인(로그인 제외)은 인각님 요청으로 해제 — BATON 주의 갱신. 역할 3종 실측 통과(사장=전체+탭 / 2공장 인력=2공장만+타공장 접근 시 되돌림 / 기사=내 티켓만).
- 피드백 8호: **업체별 이슈 추적 페이지**(/client/N — 진행 중·완료 이력·업체별 지표. "이 거래처에서 뭐가 반복되나"가 이 시스템의 최종 질문이라 자연스러운 종착). 예시 티켓을 7건으로 늘려 **제2공장에도 채움**(완료 2건 포함 — 평균 처리 시간이 살아남). **데모 사진 5장은 Pillow로 그려서 생성**(make_demo_photos.py — 천 결 + 얼룩. 실사진을 지어낼 수는 없으니 "그린 것"임을 코드와 기록에 명시).
- **인각님이 flow 캡처를 폴더에 넣어줌 → 그 구조대로 2차 개편.** 흰 사이드바(주 버튼 「＋컴플레인 접수」 + 접히는 섹션: 공장/모아보기/최근 업데이트) + 상단 바(티켓 검색 + 아바타). flow의 「최근 업데이트」 자리엔 실제 최근 티켓 3건이 색 점과 함께 뜬다. 아이콘 레일은 제품이 하나뿐이라 뺌. 검색 기능(/search — 내용·거래처 글자 검색)이 이 개편에서 따라 생김. 컴포넌트 용어(사이드바·모달·아바타 등)를 인각님께 정리해 드림 — 캡처 소통용 어휘.
- 피드백 6호: 공장 중심 화면 + 품목 모델. 대시보드에 공장 탭(전체/1/2 — 각 공장 모니터는 자기 것만 보면 된다는 인각님 요구), 공장 상세 페이지(거래처·특이사항·현재 이슈·인력 명단). **품목 표 2장 신설**(items 사전 + client_items 거래처별 취급 품목·하루 수량) — "모텔엔 가운·발수건이 없다"는 인각님 도메인 지식이 표 구조가 됨. 1공장 기사 4명으로 증원(노선 배정), 인력에 담당 업무·연락처(가짜)·입사일 추가 — 연락처 칸은 v2 SMS의 자리. 표는 총 8장.
- 사고 1건: 딕셔너리 키 이름을 items로 지어 내장 기능과 충돌(공장 페이지 500) — goods로 바꿔 해결. 예약된 이름을 피하라는 교훈.
- 피드백 5호: 실감 나는 데이터 — **공장 표 신설**(2공장 체제: 제1공장 8업체·공장장1+16, 제2공장 2업체·공장장1+4), 거래처 10곳에 업태·객실수·하루 물량 추정(객실×가동률70%×4kg) 부여, 「거래처 현황」 페이지 추가. **업체명은 실존 실명 대신 유형·규모만 본뜬 가상으로** — 실명에 지어낸 컴플레인을 붙여 공개 배포하면 허위 기록이 되므로(인각님 "실제 기반" 요청에서 이 부분만 조정, 사유 설명함). 표 구조가 바뀌어 시험용 DB는 백업(db-old-20260807.bak) 후 새로 생성.
- 피드백 3호: 사진 여러 장. 한 칸에 파일명들을 욱여넣지 않고 **photos 표를 새로 둠(조치:사진 = 1:N, 정규화)** — 이 프로젝트가 DB 설계 증명용이라 지름길 대신 정석을 택함. 옛 photo 칸의 데이터를 새 표로 옮기고 칸을 없애는 이관까지 실행·검증(기존 사진 1장이 새 표로 넘어감).
- v1 완성·검증 통과. 화면 5개 전부 200, 핵심 흐름(기사 확인 → 상태 「확인됨」) 실측 확인. 인각님 판정 대기 상태로 넘김.
- SMS 알림은 v1에서 뺌. 왜: 유료·발신번호 등록 절차가 붙는 부품이라 증명 단계에 부적합 — 화면 기반 알림(모니터 대시보드 + 담당자 페이지)으로 먼저 검증하고 발송 함수 자리만 비워둔다.
