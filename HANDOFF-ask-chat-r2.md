# 🤝 HANDOFF r2 — 세탁 상담 챗 (데스크탑 → 노트북 회신)

> 2026-08-10, **데스크탑(laundry-claim) 세션이 작성.** HANDOFF-ask-chat.md에 대한 회신.
> 데스크탑 몫은 끝났고, 터널 실연결 종단 시험까지 했다. 시험에서 나온 발견 2건과
> 계약 확장 제안 1건을 노트북 세션에 넘긴다.

## 데스크탑 몫 — 완료

- `GET /ask`: **LLM 챗 스타일 UI**(말풍선 스레드·타이핑 점 3개·하단 고정 알약 입력줄·Enter 전송,
  한글 IME 조합 중 오발송 방지). 대화는 화면에만 쌓이고 요청은 계약대로 단발 독립.
- `POST /api/ask` 프록시: 로그인·2~500자 검증, X-Brain-Token 첨부, 타임아웃 120초,
  실패 시 "상담원이 쉬고 있습니다" — 사이트 다른 기능 영향 0 확인.
- `ask_log` 표(Neon): 질문·답·성공 여부 기록 중.
- 답의 「※ 현장 검증 전」·「⛔」 딱지는 textContent로만 그려 **가공 없이 원형 표시** (계약 준수).
- BRAIN_URL 미설정이면 메뉴 자체가 숨음 — 현재 배포 상태가 이것.

## 종단 시험 결과 (임시 trycloudflare 터널, 2026-08-10)

| 질문 | 결과 |
|---|---|
| "커피 얼룩은 어떻게 빼나요?" | ✅ 7.9초, 정상 답 + 근거 + 딱지 |
| "수건에서 꿉꿉한 냄새가 나요" | ⚠️ 거절됨 — 발견② |
| "박기사가 오늘 해야 할 일이 뭐야?" | ❌ 거절 안 함 — 발견① |

## 🔴 발견① — 범위 밖 질문에 거절 대신 "지식을 끌어다 지어냄"

"박기사가 오늘 해야 할 일이 뭐야?"(업무·일정 질문 — 지식 밖)에 거절하지 않고,
녹 얼룩 조언을 끌어다 **"박기사가 오늘 해야 할 일은 … 구연산 물을 떨어뜨려 보는 것"**이라고
답했다(근거: 조언3·얼룩-녹). 사서 방식의 핵심 계약("지식 밖이면 거절")이 이 유형에서 뚫린다.

- 제안: 거절 가드에 **업무·일정·사람(누가/오늘/할 일) 유형**을 추가하고,
  합격선 시험에도 이 유형 문항을 넣어 재검증. (지어냄 0이 합격선인데, 이 답은 지어냄이다)

## 🟡 발견② — 일상어 매칭 실패

"수건에서 꿉꿉한 냄새가 나요"가 거절됐다. 곰팡이는 18종 지식에 있으므로
"꿉꿉한 냄새 → 곰팡이" 연결이 안 된 것으로 보인다. 현장 언어(냄새·꿉꿉함·쉰내)와
지식 제목의 매칭 보강 후보.

## 💡 계약 확장 제안 (v2) — "오늘 할 일" 질문을 진짜로 답하게

인각님 요청: 박기사가 "오늘 해야 할 일이 뭐야"라고 물으면 **그 사람의 배정 티켓**을 알려주기.
데이터는 사이트(DB)에 있으므로, 구조는:

- 요청 확장: `{"question": "...", "context": {"user": "박기사(배송기사)", "tickets": [{"id": 1, "client": "그랜드한강호텔", "status": "확인 대기", "content": "...", "instruction": "..."}]}}`
- 사이트가 로그인 사용자의 미완료 티켓·지시를 요약해 context로 첨부 (데스크탑 몫 — 합의되면 바로 구현)
- 모델은 **context가 있으면 그것만을 근거로** 업무 답변, context 밖 추측 금지(발견①의 가드와 한 몸)
- context가 없으면 지금과 동일(지식 사서)

발견①의 가드 보강과 이 확장은 같이 설계하는 게 맞아 보인다 — "업무 질문은 context 있으면 답,
없으면 거절"이 한 규칙이 된다.

## 참고

- 임시 터널로 시험만 했고 **Render 환경변수는 아직 미등록** — trycloudflare 주소는 터널 재시작마다
  바뀌므로, 상시 주소(고정 터널 또는 부팅 자동 시작 구성)가 나온 뒤 등록이 맞다.
- 데스크탑 쪽 기록: `worklog/2026-08-10.md`
