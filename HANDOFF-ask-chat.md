# 🤝 HANDOFF — 세탁 상담 챗 연결 (노트북 → 데스크탑)

> 2026-08-10, **노트북(laundry-stain 프로젝트) 세션이 작성해 git으로 전달.**
> 이 문서만으로 데스크탑에서 laundry-claim 쪽 작업을 시작할 수 있게 쓴 자기완결 문서.
> 이 프로젝트의 기존 흐름(BATON.md)은 건드리지 않았고, BATON 「다음 할 일」에 한 줄만 추가했다.

## 배경 (1분 요약)

- 노트북에는 **세탁공장용 상담 LLM**이 산다: Ollama + Qwen3 8B, "사서 방식" —
  감수된 지식(세탁 공정 조언 12개 + 얼룩 처리법 18종)만으로 답하고, 지식 밖이면 거절한다.
  위험 답변(약품 금지 조합 등)은 프롬프트가 아니라 **코드 가드가 차단**한다.
  사전 등록 합격선 시험(90문: 위험 0 / 조언 연결 20/20 / 얼룩 연결 19/20 / 지어냄 0)을
  통과한 구성이 배포본이다.
- 인각님 결정(2026-08-10): **이 사이트(laundry-claim)에 채팅창을 붙여 사용자가 그 모델과
  대화하게 한다.** 구조 = 노트북이 두뇌, Render가 얼굴 (건당 비용 0 원칙).
- 사진은 2단계: 채팅에 사진을 붙이면 노트북으로 넘겨 **노트북에 저장** — Render의 사진
  비영속 문제가 사라지고, 그 사진이 얼룩 판독 모델의 학습 데이터가 된다. 1단계는 텍스트만.

## 아키텍처

```
사용자 폰/PC ↔ Render(laundry-claim: 채팅 UI + 프록시) ↔ Cloudflare Tunnel ↔ 노트북(triage_server → Ollama)
```

- 노트북이 꺼져 있으면 **채팅만** "상담원이 쉬고 있습니다"가 되고 사이트 나머지는 정상.
- Render 무료 15분 잠듦은 기존과 동일 — 채팅과 무관.

## 📜 API 계약 (노트북이 제공 — 노트북 세션이 이 스펙을 보장한다)

`POST {BRAIN_URL}/api/ask`

- 헤더: `Content-Type: application/json`, **`X-Brain-Token: {BRAIN_TOKEN}`**
- 요청 본문: `{"question": "질문 텍스트"}` — 2~500자
- 응답 200: `{"ok": true, "answer": "답변 텍스트", "sources": [{"n": 1, "title": "...", "badge": true}]}`
  - `answer`에는 "※ 현장 검증 전" 딱지와 "⛔" 안전 경고가 포함될 수 있다 — **그대로 표시할 것**
    (지우면 안 됨 — 안전·정직성 장치다). `sources`는 비어 있을 수 있다.
- 응답 400: `{"ok": false, "error": "질문은 2~500자로 적어주세요"}` 등
- 응답 401: 토큰 불일치 (노트북 쪽에 토큰 인증 추가 예정 — 노트북 세션 담당)
- 응답 502: `{"ok": false, "error": "답변기를 부르지 못했습니다..."}` (Ollama 미기동)
- 응답 시간: 워밍업 후 **3~8초** (모델이 잠들어 있던 첫 질문은 최대 ~2분) → **타임아웃 120초 권장**
- 연결 실패(노트북 꺼짐/터널 끊김) → 사이트는 "지금은 상담원이 쉬고 있습니다. 잠시 후 다시
  시도해 주세요." 같은 친절한 문구로.

## 데스크탑(laundry-claim)이 할 일

1. **`GET /ask` — 채팅 페이지** (기존 화면 패턴 그대로: 쿠키 `uid` 로그인 필수, base.html 상속,
   모바일 우선)
   - 화면 문구는 **"세탁 공정과 얼룩에 대해 물어보세요"** — "무엇이든"이 아니다. 이 모델은
     지식 밖이면 거절하는 사서이고, 그건 결함이 아니라 설계다.
   - 단발 질문-답 구조 (대화 기억 없음 — 멀티턴은 사용 로그 보고 나중에 결정).
2. **`POST /api/ask` — 프록시**: 로그인 확인 → 길이 검증 → `BRAIN_URL`로 중계(토큰 첨부) →
   응답 반환. 환경변수 **`BRAIN_URL`, `BRAIN_TOKEN`** (Render 대시보드 등록).
   - `BRAIN_URL` 미설정이면 채팅 메뉴 숨김 또는 "준비 중" — **사이트 다른 기능에 영향 0**이어야 함.
3. (권장) 질문·답을 DB에 로그 — 어떤 질문이 들어오는지가 다음 개선의 나침반.

## 노트북 쪽이 할 일 (노트북 세션 담당 — 정보 공유용)

- `/api/ask`에 `X-Brain-Token` 인증 추가 · Cloudflare Tunnel 상시 구동 · 부팅 자동 시작.
- 완료되면 **BRAIN_URL(터널 주소)과 BRAIN_TOKEN 값을 인각님이 Render 환경변수에 등록** →
  재배포 → 전 구간 시험.

## 순서 제안

1. **데스크탑**: /ask 화면 + 프록시 먼저 구현 (BRAIN_URL 없이도 "준비 중"으로 배포 가능 —
   노트북 작업과 병렬)
2. **노트북**: 토큰 + Tunnel → 주소 발급
3. **인각님**: Render 환경변수 2개 등록 → 재배포 → 휴대폰에서 실제 질문 시험

## 참고

- 노트북 쪽 전체 기록: `laundry-stain/WORKLOG.md` (2026-08-10 — 모델 구성·합격선·시험 이력)
- 모델이 답하는 범위: 공정 조언 12(표백 순서·헹굼·온도 등) + 얼룩 18종(김치·기름·혈액·잉크·
  황변·녹·곰팡이·화장품류·선크림·매니큐어·커피류). **답하지 않는 것**: 키판 조작·기계 고장·
  요금·정확한 약품 농도 (전부 의도된 거절 — 판매처/기사 안내로 답함).
