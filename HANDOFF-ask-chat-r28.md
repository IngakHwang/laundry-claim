# 🤝 HANDOFF r28 — 세탁 상담 챗 (노트북 → 데스크탑): debug_delay 훅 준비 완료 — 프로브 시작해도 된다

> 2026-08-13 오후, **노트북(laundry-stain) 세션 작성.** r27 §3 제안 수용 — 시험 전용 지연
> 훅을 배치·검증했다. 필드 이름은 그쪽 제안 그대로 **`debug_delay`**. **데스크탑 구현 변경 = 0**
> (프로브는 그쪽 스크립트).

## 1. 훅 계약 (배포·검증 완료)

- **요청**: POST /api/ask, JSON에 `"debug_delay": N` (초, 숫자). **X-Brain-Token 필수** —
  틀리거나 없으면 401 (로컬 요청이라도 토큰 없인 거부. 사이트 실사용 경로엔 이 필드가 없다).
- **동작**: N초 자고 고정 응답 `{"ok": true, "probe": true, "answer": "📡 시험 지연 응답 — N초…"}`.
  상한 130s(그 위는 잘라서 잔다). question 필드 불필요. **ask_log에 안 남는다** — 사용 관찰
  통계를 더럽히지 않기 위해서니 프로브 횟수는 마음껏.
- 서버는 ThreadingHTTPServer — 프로브가 자는 동안 실사용 턴은 막히지 않는다.
- 검증 실측: 무토큰 401 ✓ · 토큰+3s = 정확히 3.0s 응답 ✓ · 일반 질문 회귀 없음 ✓ ·
  외부 왕복 403 ✓. **70/85/95/105/115s 프로브 시작해도 된다.**
- 훅은 **실측 끝나면 제거**한다 (우리 잔무에 등록). 결과가 나오면 다음 HANDOFF로.

## 2. §2 답 — 사진+글 턴은 순차가 맞다 (코드 확인)

- 순서: 글 답변 생성(텍스트 사서) → 판독(VLM) — 모델 교대를 한 번으로 줄이는 r19 부하
  규칙 때문에 **순차 고정**이다. 사진만·"이건 뭐야?" 지시형 턴은 pre_filter 고정 답이라
  LLM 생성이 없어 ≈ 판독 시간뿐 (그쪽 48.0s 실측이 이 유형).
- 따라서 예산 인상이 확정되면 그쪽 짐작대로 **턴별 차등**으로 간다: 사진만 = 새 예산 전액,
  사진+글 = `한도 − 글 생성 소요 − 여유` 잔여 예산. 구현은 판독 호출 직전에 남은 시간을
  계산해 넘기면 돼서 작다. 수치는 실측 한도가 나온 뒤 **인각님 등록**으로 확정.

## 3. 상태

- 남은 순서: ① 그쪽 프로브 → 끊기는 지점 확정 ② 판독 예산·턴별 차등 수치 인각님 결정
  ③ 배포 + 훅 제거. 관찰 지속 항목 변화 없음.
