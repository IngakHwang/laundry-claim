# 🤝 HANDOFF r4 — 세탁 상담 챗 (데스크탑 → 노트북)

> 2026-08-10 밤, **데스크탑(laundry-claim) 세션이 작성.** r3 잘 받았다(가드 봉쇄·90문 바이트 동일 검증 방식 좋았다).
> 인각님의 새 요구 4건을 정리해 넘긴다. 데스크탑 쪽 선행 작업은 끝났다.

## 데스크탑 신규 완료 (r3 이후)

- **대화방 여러 개 + 대화내역 DB 저장** (chats/chat_msgs, Neon). 사용자가 주제별로 방을 파고,
  새로고침·재방문에도 대화가 남는다. **→ 이 저장된 이력이 아래 A(멀티턴 기억)의 재료다.**

## 인각님 요구 4건 (2026-08-10 밤)

A. **후속 질문 기억** — 같은 방에서 "그럼 찬물은?"이 통해야 한다
B. (암묵) 체감 속도 — 챗 표준 논의에서 스트리밍 필요성 확인
C. **이미지 입출력** — 얼룩 사진을 찍어 물어보기 (출력은 데스크탑 판단: 불필요)
D. **원리(왜) 질문 + 인터넷 검색** — "왜 피는 과산화수소를 써야 해?"에 답할 수 있나,
   못 하면 검색해서 알려줄 수 있나

## 실증 (D 관련, 터널 직접 질의 2026-08-10 밤)

- "왜 피 얼룩에는 과산화수소를 써야 하나요?" → 거절하지 않고 혈액 지식에서 답을 조립:
  "락스는 누런 얼룩을 만들므로 과산화수소. **과산화수소는 피 속의 철분을 제거하는 데 도움**" (6.2초)
- 같은 모델의 "피 얼룩 어떻게 빼나요?" 답은 "**갈색 잔여 = 철분 → 산성 처리**"라고 함.
- **→ 두 답이 철분 처리를 놓고 모순.** 화학적으로는 과산화수소의 역할이 유기 색소 산화이지
  철분 제거가 아니므로 앞 답이 부정확해 보인다. "왜" 수준 지식이 얕아 반쯤 조립된 답이
  나오는 상태 — 원리(화학) 지식이 지식 항목에 명시돼야 정확해진다.

## 제안 (우선순위 순)

### A+업무: context 확장 계약에 「대화 이력」을 같이 싣자 (최우선)

r3에서 합의한 context 스케치를 한 칸 넓히는 것:
```
{"question": "...",
 "context": {
   "user": "박기사(배송기사)",              // 있으면 업무 답변 허용 (r3 합의)
   "tickets": [...],                        // 로그인 사용자의 미완료 티켓 (r2 제안)
   "history": [{"role": "user"|"bot", "content": "..."}]   // 이 방의 최근 N턴 (신규)
 }}
```
- 모델 규칙(제안): history는 "직전 대화 맥락"으로만 참고(후속 질문 해석용), 지식·근거는 현행 그대로.
  tickets 없는 업무 질문 거절도 현행 유지.
- 데스크탑은 세 필드 모두 **조립 준비 완료** — 계약 확정만 되면 바로 붙인다.
- 합격선: 후속 질문 문항(대명사·생략 주어)을 추가하면 될 듯. **재등록 배치(94문+α)에 합류 제안.**

### B: 스트리밍 (2순위 — 체감 속도를 가장 크게 바꿈)

- 제안 계약: `POST /api/ask`에 `"stream": true`면 응답을 chunked text로(Ollama stream 그대로 중계),
  마지막 줄에 sources JSON. 데스크탑 프록시·화면은 준비할 수 있다.
- 노트북 부담이 크면 후순위로 미뤄도 된다 — 판단 위임.

### C: 이미지 입력 (2단계 본안 — 시기 판단 위임)

- 데스크탑: 채팅에 사진 첨부(multipart) + 중계 준비 가능. 노트북: 저장(학습 데이터) 먼저,
  VLM 판독은 그 다음 단계로 나눠도 됨.

### D: 원리(왜) 지식 + 인터넷 검색의 취급 — ⚠️ 인각님 결정 항목

데스크탑 의견: 검색 결과를 **사용자에게 바로 답으로 주는 것은 반대.**
- 이유: 사서 방식의 존재 이유(감수된 지식만·지어냄 0)가 무너지고, 위험 조합 가드가
  검색 결과에는 안 걸린다(세탁 약품은 안전 문제 — 락스+산성 유독가스 류).
- 대안 제안: **검색 → 감수 → 지식 추가 파이프라인.** 모델이 답 못 한 질문(거절 로그)과
  얕게 답한 질문을 모아, 검색으로 후보 지식 초안을 만들고, 인각님 감수를 거쳐
  합격선 재시험과 함께 지식에 넣는다. 위 실증의 「혈액 원리」가 첫 후보
  (철분 모순 교정 + 과산화수소 산화 원리 + 찬물 이유).
- 절충안(원하시면): 감수 전 정보를 보여줘야 한다면 모델 답과 분리된
  「⚠️ 감수 전 인터넷 참고」 섹션으로 — 단, 데스크탑은 여전히 파이프라인 쪽을 권함.

## 정리 — 결정 대기 목록 (인각님)

1. 합격선 재등록 배치 범위: 기존(냄새 1 + 업무 3) + **A의 history + D의 혈액 원리** 포함 여부
2. Cloudflare 계정 (상시 터널 — r3에서 대기 중)
3. D의 방침: 파이프라인(권장) vs 감수 전 표시 절충안
