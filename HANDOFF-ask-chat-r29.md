# 🤝 HANDOFF r29 — 세탁 상담 챗 (데스크탑 → 데스크탑→노트북): 게이트웨이 실측 결과 — 한도 미발견, 병목은 프록시 120s

> 2026-08-13, **데스크탑(laundry-claim) 세션 작성.** r28의 GO를 받아 실측했다. 단, 방법을
> 하나 바꿨다(§1 첫머리) — debug_delay는 실배포 사슬로는 닿을 수 없어서다(내 r27 설계 결함).

## 1. 방법 정정 + 실측 결과

- **정정**: `debug_delay`는 프록시가 payload를 자기 손으로 조립하므로 사이트 경유로는 노트북에
  못 닿는다(question·history·context·image만 실림) — r27 §3을 낸 내 설계 실수다. 그래서
  게이트웨이 구간은 **앱에 임시 계측 라우트**(/admin/probe-sleep, 사장 전용, N초 자고 200)를
  얹어 쟀고, 실측 후 즉시 제거했다(커밋 2개: 추가·제거).
- **결과 (실배포, 브라우저→게이트웨이→앱 경로)**:

| 지연 | 결과 |
|---|---|
| 70 / 85 / 95 / 105 / 115s | 전부 완주 (연결 유지, n+1.5s에 응답 수신) |
| 115s · 125s (상태코드 확정 재발) | **HTTP 200**, body 정상 |

- **결론: Render 게이트웨이는 125s까지 요청을 안 끊는다. 통설 ~100s는 이 서비스에 해당
  없음.** 사슬의 실제 병목 = **우리 프록시 urlopen timeout 120s** (우리 설정값이라 조정 가능).

## 2. 남은 미지수 1 — 터널 구간 (노트북 몫)

- 내 프로브는 앱 직행이라 **터널(ngrok)을 안 탔다.** 터널 에지가 긴 응답을 끊는지는
  노트북이 자기 공개 URL로 `debug_delay` 95~110s 외부 프로브 한 번이면 확정된다 (훅이
  이미 있으니 1분짜리 일). 그게 확인되면 사슬 전체가 닫힌다.
- 참고로 실사용 62.2s 통과는 이미 확인돼 있다(내 12장 시험).

## 3. 권고 (수치 확정은 인각님 등록)

- 터널만 무사하면 **판독 예산 90s GO** — 게이트웨이 여유는 충분하다.
- 사슬 예산 상한은 「프록시 120s − 여유 10s = 110s」로 잡으면 되고, r28 §2의 턴별 차등
  (사진만 = 전액, 사진+글 = 잔여)은 그 안에서 노트북 설계대로.
- 프록시 120s를 늘릴 필요는 지금 없다고 본다 — 110s 사슬 예산이면 후퇴율 처방에 충분.

## 4. 상태

- 임시 라우트 제거 완료(배포됨). debug_delay 훅 제거는 터널 프로브 뒤 노트북 잔무 그대로.
- 순서 남은 것: ① 노트북 터널 프로브 → ② 인각님 수치 등록 → ③ 배포+훅 제거.
