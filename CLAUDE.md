# laundry-ticket — 세탁공장 컴플레인 티켓 시스템

## 이 프로젝트가 뭐냐
세탁공장이 전화·문자로 받던 거래처 컴플레인을 **기록 → 담당자 배정·전달 → 조치 → 완료**까지 추적하는 작은 웹 서비스.

- 기원: 2026-08-07, 인각님 설계. 총괄 시절의 실제 문제에서 출발 — 컴플레인이 전화·문자로 오가서 ①기록이 안 남고 ②전달 확인이 안 되고 ③처리 이력이 흩어졌다. 배송기사가 투잡·프리랜서라 공장 사정을 모르는데, 특이사항 전달이 매번 사장의 전화 한 통이었다.
- **목적 = 운영이 아니라 증명.** 라이넨스 등 채용의 자격요건 「API·DB·클라우드로 서비스 구축」을 작동물로 채우는 것. 공고의 업무 축 중 품질(세탁 컴플레인)·수배송(배송 컴플레인) 둘을 덮는다.
- 구조의 이름 = 티켓 시스템. 책이 아니라 공장 경험에서 도출했다는 것이 이 프로젝트의 이야기다.

## 핵심 원칙
1. **v1은 증명 범위만.** 로그인 X, 문자(SMS) 발송 X(유료·발신번호 등록 절차 — v2 카드, 발송 함수 자리만 비워둔다), 푸시 X. 알림 = ①공장 모니터 대시보드(자동 새로고침) ②담당자 자기 페이지("내게 배정된 티켓") + 확인 버튼.
2. **조치 기록(actions)은 이력 보존형 장부.** "어떻게 해야 하는지(지시)·어떻게 했는지(처리)"가 시간순으로 쌓이는 일지. 본인이 손으로 쓴 기록만 수정·삭제할 수 있고, 수정하면 원문이 이력(action_edits)으로, 삭제하면 취소선으로 남는다. 남의 기록은 role 불문 못 건드린다. (2026-08-12 인각님 결정 — 순수 append-only에서 완화. 장부 원칙의 핵심 「흔적 보존」은 유지)
3. 스택 = Python 3.12 + FastAPI + SQLite + 서버 렌더링(Jinja2). 폰 화면 우선(기사는 폰으로 본다). 클라우드 배포는 v1 모양이 확정된 뒤(무료 티어 조건은 그때 확인).
4. 코드는 Claude가 작성하되 **변수·함수마다 상세 한글 주석** — 인각님이 읽고 설명할 수 있어야 한다(면접에서 답하는 사람은 인각님).
5. 실행 = `.venv\Scripts\python -m uvicorn app:app --reload`.

## 표 네 장 (v1)
- `clients` 거래처 · `staff` 담당자(공장/기사 구분 하나) · `complaints` 컴플레인(거래처·유형=품질/배송/기타·내용·심각도·상태·담당자) · `actions` 조치 기록(어느 티켓에, 누가, 지시/확인/처리/상태변경, 내용, 시각)
- 상태 흐름: 접수 → 확인됨(담당자가 봤다) → 처리중 → 완료

## 기록 (BATON + worklog)

**파일 둘로 갈린다 — 미래는 `BATON.md`, 과거는 `worklog\<날짜>.md`.** (2026-07-28 기록 분리 원칙)

- **`BATON.md`** = **아직 안 끝난 것만.** 훅이 세션 시작에 자동으로 넣어준다. 칸 셋 = `🎯 다음 할 일`(하면 끝남) · `❓ 인각님이 답할 것`(정해야 끝남) · `⚠️ 주의`(안 없어짐).
  - **끝나면 그 줄을 지운다.** ⚠️2,000자를 넘으면 안 지우고 있다는 신호다(훅이 경고한다).
  - 「주의」만은 기록과 겹쳐도 된다 — **한 줄 + 날짜**로만 두고 사정은 그날 기록에.
- **`worklog\YYYY-MM-DD.md`** = **그날 기록.** 세션을 열면 훅이 그날 파일을 만든다(없을 때만). 작업 안 한 날은 파일이 안 생긴다.
  - **무엇을** = **"왜"만.** "무엇이 바뀌었나"는 git 이력에 이미 있다 — 두 번 쓰지 않는다.
  - **언제** = 결정·전환·막힘마다 그 자리에서 이유와 함께 즉시 append. **세션 끝 소급 작성 금지.**
  - **한번 적으면 안 고친다.**
- **⚠️ WIKI 일지에는 쓰지 않는다.** WIKI로 갈 것은 하나뿐 — **이 프로젝트를 넘어서는 배움.** 그때도 `📥 Inbox.md`에 **`[laundry-ticket] 한 줄`**만 던진다. 노트를 만들지 않는다 — 다듬는 일은 WIKI 세션에서 인각님이 한다.
