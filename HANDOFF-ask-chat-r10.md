# 🤝 HANDOFF r10 — 세탁 상담 챗 (데스크탑 → 노트북): 이미지 종단 시험 결과

> 2026-08-11 오후, **데스크탑(laundry-claim) 세션이 작성.** BRAIN_IMG를 켜고(인각님 Render 등록)
> 실배포 종단 시험을 돌렸다. **이미지 1단계는 개통** — 단, 회귀 1건을 발견해 아래에 보고한다.

## 1. 종단 시험 결과 (실배포, 박기사 계정, 시험 방 5·6 — Neon에서 삭제 완료)

| 시험 | 결과 |
|---|---|
| 사진+글 ("수건에 이런 얼룩이…") | ✅ 형식 정상 — 답 끝에 📷 고지 한 줄 정확히 부착 |
| 사진만 (글 없음) | ✅ **고지 한 줄만 즉답** — r9 표 2행의 설계대로. 대체 문구 "(사진만 보냄)" 경로 정상 |
| 글만 (커피 얼룩) | ✅ 기존 답변·딱지 정상 — 답 본문 회귀 없음 |

- 프록시 축소 경로도 실배포에서 통과: 회전 태그(Orientation=6) 든 3000×2000 시험 사진 →
  노트북까지 무사 도달(고지 응답으로 확인). 저장분이 세로(1067×1600)·EXIF 0건인지는
  `collected.jsonl`과 파일로 노트북 쪽에서 확인해 달라 — 시험 사진은 갈색 단색 계열 2장이다.
- 사진+글의 답 자체는 거절+인터넷 참고였는데, 이건 결함이 아니라 예상 동작으로 본다 —
  "이런 얼룩"은 사진을 가리키는 말인데 1단계 모델은 사진을 못 보므로 알 수 없는 질문이 맞다.
  고지 한 줄이 "글로 물어봐 달라"고 안내하는 것까지가 1단계의 정직한 모양.

## 2. ⚠️ 회귀 1건 — `sources` 배열이 빈 채로 온다 (노트북 확인 필요)

- 증상: 답 본문에는 「[근거: 얼룩-커피·차·음료]」 딱지가 붙는데 **응답 JSON의 `sources`가 `[]`**
  — 그래서 사이트 화면의 근거 칩이 안 뜬다.
- 이미지와 무관하게 재현된다: **새 방 + 글만** + 표준 지식 질문("커피 얼룩은 어떻게 빼나요?")
  에서도 `sources: []`.
- 시점 근거: 오늘 오전(r7 검증, 이미지 수신부 배포 전) 같은 계정·같은 유형 질문에서
  `sources: [{"n": 11, "title": "…", "badge": true}]`가 정상으로 왔다. 오후 이미지 수신부
  배포 뒤부터 빈다 — 이미지 업데이트에서 응답을 다시 조립하며 sources를 떨어뜨린 것으로 추정.
- 데스크탑 프록시는 body를 그대로 중계하므로(글만 경로는 이번에 코드가 안 바뀜) 노트북 쪽이다.

## 3. 다음

- 노트북: ①sources 회귀 수정 ②`collected.jsonl` 꼬리 확인(오늘 2장 — 시각·bytes·turns 기록됐는지,
  파일이 세로 1067×1600 JPEG인지) → 회신 r11.
- 데스크탑: sources 수정 배포되면 근거 칩 재확인만 하면 된다. 그 외 이미지 1단계는 완료로 본다 —
  폰 실기기 촬영 확인은 인각님 몫으로 남긴다(카메라 버튼 → 촬영/앨범 → 전송 → 고지 확인).
