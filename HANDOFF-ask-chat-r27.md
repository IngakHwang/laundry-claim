# 🤝 HANDOFF r27 — 세탁 상담 챗 (데스크탑 → 노트북): r26 문의 답 (대기 한도) + 합동 실측 제안

> 2026-08-13, **데스크탑(laundry-claim) 세션 작성.** r26 §3(프록시·클라이언트가 몇 초까지
> 기다리나)에 답한다. 결론 먼저 — **확실한 건 프록시 120s뿐, 게이트웨이는 문서에 없다.
> 추측으로 예산을 올리지 말고 실측 한 번 하자(§3).**

## 1. 사슬 각 단의 대기 한도 (오늘 코드·문서 재확인)

| 단 | 한도 | 근거 |
|---|---|---|
| 브라우저(챗 화면) | **자체 제한 없음** | ask.html의 fetch에 타임아웃·AbortController 없음 — 끊는 쪽은 항상 사슬 위쪽 |
| Render 게이트웨이 | **미확정 (~100s 통설)** | 공식 문서에 명시 없음(오늘 재검색). 커뮤니티 통설 ~100s. 실측은 62.2s 통과까지만 확인 |
| 프록시 → 노트북 | **120s** | app.py ask_proxy, urlopen(timeout=120) — r5부터 불변 |

- 즉 병목 후보는 게이트웨이 하나인데 값이 미확정이다. 90s 인상이 안전한지 지금은
  **아무도 모른다**가 정직한 답.

## 2. 인상 전에 확인할 것 1건 — 사진+글 턴의 합산

- 판독 예산을 90s로 올리면 **사진만** 턴은 90+α. **사진+글** 턴은 글 답변 생성과 판독이
  순차라면 합산이 게이트웨이를 넘을 수 있다 (내 실측: 사진+글 48.0s — 내역이 순차인지
  병렬인지 그쪽 로그로 확인 가능할 것). 순차라면 "사진만 90s / 사진+글은 잔여 예산" 같은
  턴별 차등이 필요할 수 있다.

## 3. 제안 — 게이트웨이 한도 합동 실측 (데스크탑 변경 0)

- 노트북 /api/ask에 **시험 전용 지연**을 잠깐 넣어달라: BRAIN_TOKEN 인증 + 요청에
  `"debug_delay": N` 필드가 있을 때만 N초 자고 정상 응답 (실사용자는 못 건드린다).
- 그러면 내가 실배포 사슬로 70/85/95/105/115s를 프로브해 **실제 끊기는 지점**을 확정한다.
  115s까지 살면 프록시(120s)가 한도라는 뜻이고, 그 아래서 끊기면 그 값이 게이트웨이다.
- 실측 후 판독 예산을 "한도 − 10s(오버헤드 여유)"로 정하면 된다. 준비되면 다음 HANDOFF에
  지연 필드 이름만 알려달라 — 프로브는 내 쪽 스크립트라 사이트 변경이 없다.

## 4. 접수 · 상태

- r26 §1 교차 검증(시트 2건 = 내부 guess, 후퇴 = 예산 초과) 접수 — "confident 6/6"을
  "단정 표기 6/6(내부 confident 4 + guess 2)"로 정정 이해했다.
- 후퇴 문구 교체("이번 사진은 판독하지 못했습니다") 배포 확인 — §2 관찰을 하루 만에
  처방까지 간 것, 방향 맞다고 본다. 인각님 폰 재시도 완료(12:15 에코백 ✓)도 접수 — BATON에서 내림.
- 데스크탑 몫: §3 프로브 대기 외 없음.
