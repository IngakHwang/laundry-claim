# 🤝 HANDOFF r9 — 세탁 상담 챗 (노트북 → 데스크탑): 이미지 수신부 구현 완료

> 2026-08-11 오후, **노트북(laundry-stain) 세션이 작성.** r8 합의 순서대로 노트북 수신부를
> 구현·배포했다. **데스크탑은 BRAIN_IMG를 켜고 종단 시험으로 넘어가면 된다.**

## 구현 완료 — `/api/ask` `image` 필드 (지금 실서버에 반영됨)

- r8 명확화 4건 전부 수용:
  1. **JPEG 고정** — base64 해독 후 JPEG 시작 표식(FF D8 FF)만 검사. PNG/WebP 분기 없음.
  2. EXIF는 데스크탑 몫 확인 — 노트북은 받은 그대로 저장.
  3. 크기: 요청 본문 한도 64KB→**8MB** 상향(사진 요청용), 사진 자체는 6MB 방어선(계약 4MB보다 여유).
  4. history에 사진 없음 — 수신부도 history는 기존 그대로 글만 다룬다.
- 저장: `data/images/collected/ask/YYYYMMDD/HHMMSS_<uuid8>.jpg` +
  같은 폴더 `collected.jsonl`에 {ts, file, question(200자), bytes, turns}.
- 응답 형식 불변 (`ok/answer/sources`). 터널 경유 토큰 규칙도 그대로.

## 동작 (실서버 시험 4종 통과)

| 요청 | 동작 |
|---|---|
| 글 + 사진 | 평소처럼 사서가 답하고, 답 끝에 고지 한 줄 추가 |
| `"(사진만 보냄)"` + 사진 | **LLM을 부르지 않고** 고지 한 줄만 즉답 (대체 문구를 지식 질문으로 오해해 엉뚱한 거절 만드는 것 방지) |
| 글 + 깨진 사진 | 글 답변은 정상 유지 + "📷 사진이 제대로 도착하지 않았습니다. 한 번만 다시 보내 주세요." (저장 안 함) |
| 글만 | **기존과 바이트 동일** — 회귀 없음 |

- 고지 최종 문구 (인각님 확정 — 배너 없음, 이 한 줄이 저장·목적 고지 겸함):
  "📷 사진은 받아뒀습니다 — 얼룩 판독 기능을 만드는 데 참고합니다. 판독 기능은 준비 중이라,
  지금은 글로 물어봐 주시면 답해 드립니다."
- LLM 프롬프트·가드는 건드리지 않았다 (서버 계층) — 합격선 재시험 대상 아님. 배포 전
  글-경로 회귀를 실서버에서 확인했다 (위 표 4행).

## 다음

- 데스크탑: 에코 검증 → **BRAIN_IMG 켜고 실배포 종단 시험** (폰 촬영 → 썸네일 → 노트북 저장
  확인까지) → 결과 r10. 노트북 쪽 확인 명령: `data/images/collected/ask/collected.jsonl` 꼬리.
