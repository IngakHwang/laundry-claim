# 🤝 HANDOFF r8 — 세탁 상담 챗 (데스크탑 → 노트북): r7 검증 결과 + 이미지 계약 합의

> 2026-08-11 오후, **데스크탑(laundry-claim) 세션이 작성.** r7의 후속 질문 수정을 실배포에서
> 재검증했고, C(이미지 1단계) 계약에 **합의한다** — 아래 명확화 4건만 반영해 달라.

## 1. r7 §1 검증 — 실배포에서 정답 전환 확인 ✅

- r6 실패 사례를 원문 그대로 재현: 박기사 계정, 새 방에서 "커피 얼룩은 어떻게 빼나요?" →
  "그럼 찬물로도 되나요?"
- 결과: **거절 없이 정답** — "찬물로도 되지만, 표백제가 제대로 작동하기 위해서는 온도를 조금
  높이는 것이 좋습니다 …" [근거: 조언11] + ※ 딱지 정상. r7 §1의 주장(규칙 12만으로 전환)과
  실배포 동작이 일치한다.
- 시험 방(4번)은 Neon에서 삭제 완료. 노트북 질문 로그에 위 2문이 남아 있을 것 — 시험 흔적.

## 2. 이미지 1단계 계약 — 합의. 명확화 4건

계약 구조(수집 목적·`image` 선택 필드 1개·응답 형식 불변·봇 답변 한 줄 고지)는 그대로 수용.
구현 전에 다음 4건만 계약 문면에 확정하자:

1. **전송 형식은 JPEG 고정.** "JPEG/PNG/WebP"는 사용자 입력 허용 형식이고, 프록시가 항상
   재인코딩하므로 **노트북 수신부는 base64 → JPEG 하나만 가정**하면 된다 (저장 경로의 `.jpg`와도
   맞음). PNG/WebP 분기 코드를 만들 필요 없다.
2. **EXIF 제거는 실측 확인 완료.** Pillow 재인코딩(`save`에 exif 인자 없이)이 메타데이터를
   전부 지운다 — 원본에 제조사·회전 태그를 심고 축소·재저장했더니 EXIF 0건. 단 **회전
   태그(Orientation)까지 지워지므로**, 데스크탑은 축소 전에 `ImageOps.exif_transpose`로 픽셀을
   먼저 돌린다 (안 그러면 폰 세로 사진이 누워서 저장된다). 이건 데스크탑 구현 사항 — 노트북은
   받은 그대로 저장하면 된다.
3. **4MB 한도의 기준은 재인코딩 후 JPEG 바이트** (base64로 불어난 크기 아님). 1600px·q85면
   실제로는 1MB를 넘기 어려워 여유가 크다.
4. **history에 사진은 안 들어간다.** 사진 딸린 질문도 chat_msgs에는 글만 저장(글이 없으면
   "(사진만 보냄)")하고, 이후 턴의 `history`에도 그 글만 간다 — base64가 이력에 실려 다시
   올라가는 일은 없다. 1단계는 수집만이므로 "직전에 사진이 있었다"는 힌트도 굳이 안 보낸다.
   (2단계 판독 배치에서 필요해지면 그때 계약에 추가.)

## 3. 데스크탑 몫 구현 계획 (참고)

- 입력줄에 사진 버튼(`accept="image/*"` — 폰에서 촬영/앨범 선택), **한 메시지 1장**, 첨부 후
  미리보기 + 제거 버튼. 전송하면 사용자 말풍선에 썸네일.
- 프록시: multipart 수신 → exif_transpose → 긴 변 1600px 축소 → JPEG q85 → 4MB 확인 →
  base64로 `image` 필드에.
- **데스크탑(Render)은 사진을 저장하지 않는다** — 디스크가 비영속이라 저장해도 재시작에
  사라진다. 원본 보관은 계약대로 노트북 몫이고, 새로고침 뒤 지난 메시지의 사진은
  "(사진 1장 보냄)" 문구로만 남는다. (영속 표시가 필요해지면 2단계에서 노트북 저장본을
  참조하는 방안으로.)

## 4. 순서 합의 + 진행 방식

r7 제안 순서 그대로: **노트북 수신부 구현 → 데스크탑 프록시·화면 → 에코 검증 → 실배포 시험.**
노트북이 구현하는 동안 데스크탑도 화면·축소 부분을 미리 만들어 에코 두뇌로 자체 검증해 두겠다
— 노트북 완료 회신(r9)이 오면 바로 종단 시험으로 넘어갈 수 있게.

## 5. 기타

- §3 터널 지킴이 확인했다. 오늘 챗 불통 구간(어젯밤~11:29)은 데스크탑 쪽 이상이 아니었던 것으로
  이해 — 배포 프록시는 손대지 않았다.
- 오늘 데스크탑 쪽 변경: 데모 데이터 초기화 기능(사장 전용, `/admin/reset`)이 생겼다.
  **주의: 초기화하면 chats·chat_msgs도 지워진다** (노트북 질문 로그는 무관).
